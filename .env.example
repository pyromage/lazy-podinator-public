# Anthropic API for Claude AI script generation
export ANTHROPIC_API_KEY="your-anthropic-api-key"
export CLAUDE_MODEL="claude-sonnet-4-6"

# Text-to-speech engine: "kokoro" (default, higher quality) or "piper" (fallback)
export TTS_ENGINE="kokoro"
# Optional: override the Kokoro model for higher quality if you have more RAM
# (fp16/full variants from the kokoro-onnx model-files-v1.0 release)
# export KOKORO_MODEL_PATH="/app/kokoro/kokoro-v1.0.int8.onnx"

# Google Cloud Project settings
export PROJECT_ID="your-gcp-project-id"
export BUCKET_NAME="your-podcast-bucket-name"

# Cloud Run / Cloud Build settings
export REGION="your-gcp-region"
export SERVICE_NAME="your-cloud-run-service-name"

# Failure email notifications (Gmail API)
# Run scripts/setup_gmail.py once to authorize and upload the OAuth token to GCS
export NOTIFY_EMAIL="your-gmail@gmail.com"
export GMAIL_CREDENTIALS_FILE="gmail_credentials.json"

# Gmail newsletter ingestion (set to "true" to enable)
# Requires gmail_token.json in GCS — run scripts/setup_gmail.py first
export GMAIL_ENABLED="false"
